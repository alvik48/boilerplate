# Backend Template

Copy this directory into `apps/backend.<name>`, rename the package, then run
`pnpm install` from the repository root.

The template is a minimal NestJS service with TypeScript, ESLint, Prettier, and
basic health routing.
