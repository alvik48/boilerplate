# docs-core

Framework-independent content manifest, metadata, links, integration coverage and scoped search.

Consume explicit package exports; build with `pnpm --filter @packages/docs-core build`.
Run unit tests with `pnpm --filter @packages/docs-core test` after building dependencies.

See [documentation architecture](../../docs/architecture.md) and
[authoring rules](../../docs/repository/documentation.md).
