# mcp

Official SDK documentation server, tool descriptors and injected product authorization/handlers.

Consume explicit package exports; build with `pnpm --filter @packages/mcp build`.
Run unit tests with `pnpm --filter @packages/mcp test` after building dependencies.

See [documentation architecture](../../docs/architecture.md) and
[authoring rules](../../docs/repository/documentation.md).
