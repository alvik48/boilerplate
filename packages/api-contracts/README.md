# api-contracts

Nest OpenAPI configuration, artifact validation and readable operation contracts.

Consume explicit package exports; build with `pnpm --filter @packages/api-contracts build`.
Run unit tests with `pnpm --filter @packages/api-contracts test` after building dependencies.

See [documentation architecture](../../docs/architecture.md) and
[authoring rules](../../docs/repository/documentation.md).
